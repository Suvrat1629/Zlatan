<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- GNSS position + GnssStatus (NavIC/IRNSS constellation, satellite count) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Without this, Android 12+ caps motion sensors below 200 Hz — we want the
         highest reliable rate (docs/architecture-android.md §4). -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- EngineService must survive backgrounding / screen off (§8). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Only the map tile layer needs this (F4-dependent); the engine itself needs zero
         network (docs/App-Stack-and-Integration-Decisions.md §5). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="28" />
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.IDR">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MagnetometerCalibrationActivity"
            android:exported="false"
            android:label="@string/calibration_title" />

        <!-- Single owner of engine lifetime (docs/architecture-android.md §8) — Activities
             bind and observe, never create engine state. -->
        <service
            android:name=".EngineService"
            android:foregroundServiceType="location"
            android:exported="false" />

    </application>
</manifest>
